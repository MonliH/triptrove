import React from 'react';
import { Heading } from '@chakra-ui/react';

type EndProps = {
    loading:boolean;
};

const End:React.FC<EndProps> = ({loading}) => {
    
    return (
     <h1></h1>
        )
}
export default End;